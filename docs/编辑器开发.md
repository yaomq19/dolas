# Dolas 编辑器开发路线图

## 项目概述

本文档详细规划了 Dolas 游戏引擎编辑器的开发计划，基于现有的 ImGui 框架进行增强。开发分为多个阶段，优先实现资源管理、场景编辑和场景保存/加载等核心功能。

**技术栈**：
- ImGui + ImGuizmo（3D Gizmo 库）
- DirectX 11 渲染后端
- C++20
- CMake 构建系统

**UI 风格**：类似 Unity/Unreal 的深色主题专业编辑器风格

---

## 阶段一：资源管理系统（第1-2周）

### 任务1.1：集成 ImGuizmo 库
```
优先级：高
工作量：0.5天

状态：✅ 已完成 (2026-03-10)

操作步骤：
1. 添加 ImGuizmo 到 third_party（或使用直接集成）
2. 更新 CMakeLists.txt
3. 编写简单的集成测试
```

**完成详情**：
- ✅ ImGuizmo 添加为 Git Submodule
- ✅ CMakeLists.txt 更新完成
- ✅ 编译成功，生成 `imguizmod.lib`
- ✅ DolasFunction 成功链接 ImGuizmo
- ✅ 简单测试代码添加完成

详细报告：见 `docs/ImGuizmo集成报告.md`

### 任务1.2：资源浏览器 - 核心功能
```
优先级：最高
工作量：4-5天

功能清单：
✓ 扫描 content/ 目录结构
✓ 显示文件/文件夹树形列表
✓ 文件类型图标（根据扩展名）
✓ 网格/列表视图切换
✓ 文件选择和预览
✓ 路径导航（面包屑、前进/后退）
✓ 刷新按钮
```

**实现架构**：
```cpp
// src/engine_runtime/dolas_function/public/include/editor/ContentBrowser.h
class ContentBrowser
{
public:
    void Render();
    void Refresh();

    std::string GetSelectedPath() const;

private:
    void RenderDirectoryTree();
    void RenderFileList();
    void RenderToolbar();

    void ScanDirectory(const std::string& path);

    std::string m_current_path;
    std::string m_selected_path;
    std::vector<FileInfo> m_files;

    bool m_grid_view = true;
};
```

### 任务1.3：资源浏览器 - 预览功能
```
优先级：高
工作量：3-4天

功能清单：
✓ 纹理预览（显示图片）
✓ 模型预览（简单网格渲染）
✓ 材质预览（材质球）
✓ 着色器代码预览（文本）
✓ 双击打开外部编辑器
```

**技术细节**：
- 纹理预览：使用 ImGui::Image 显示 Texture 资源
- 模型预览：创建独立的预览 RenderView，在单独窗口渲染
- 需要添加 `ResourcePreviewManager` 类

### 任务1.4：资源导入系统
```
优先级：高
工作量：3-4天

功能清单：
✓ 导入对话框（文件选择）
✓ 支持格式：FBX/OBJ（模型）、PNG/JPG/TGA（纹理）
✓ 导入设置面板（缩放、材质生成等）
✓ 批量导入
✓ 导入进度显示
✓ 覆盖确认对话框
```

**实现架构**：
```cpp
class ResourceImporter
{
public:
    void ShowImportDialog();
    void ImportFiles(const std::vector<std::string>& files);

private:
    void ImportModel(const std::string& file);
    void ImportTexture(const std::string& file);

    ImportSettings m_settings;
};
```

### 任务1.5：资源浏览器 - 拖放功能
```
优先级：中
工作量：2-3天

功能清单：
✓ 拖拽纹理到材质槽
✓ 拖拽模型到场景
✓ 拖拽材质到实体
✓ 拖拽着色器到材质
```

---

## 阶段二：场景编辑系统（第3-4周）

### 任务2.1：选择系统 - 核心功能
```
优先级：最高
工作量：3-4天

功能清单：
✓ 鼠标射线拾取实体
✓ 点击选择单个实体
✓ 框选多个实体
✓ 选中高亮显示（包围盒）
✓ 层级选择（父/子）
✓ Ctrl 多选
✓ 取消选择（点击空白处）
```

**技术实现**：
```cpp
class EditorSelection
{
public:
    void UpdateSelection(const Vector2& mouse_pos);

    void SelectEntity(RenderEntityID id);
    void SelectEntities(const std::vector<RenderEntityID>& ids);
    void DeselectAll();

    const std::vector<RenderEntityID>& GetSelectedEntities() const;

private:
    RenderEntityID RayCast(const Vector2& mouse_pos);

    std::vector<RenderEntityID> m_selected_entities;
};
```

### 任务2.2：Gizmo 控制器
```
优先级：最高
工作量：3-4天

功能清单：
✓ 移动 Gizmo（W 键）
✓ 旋转 Gizmo（E 键）
✓ 缩放 Gizmo（R 键）
✓ Gizmo 模式切换
✓ 世界/本地空间切换
✓ 轴锁定（按住 Shift）
✓ 精确输入（数字输入框）
✓ Gizmo 大小自适应
```

**集成 ImGuizmo**：
```cpp
class GizmoController
{
public:
    void Render(const Matrix4x4& view, const Matrix4x4& proj);

    void SetMode(ImGuizmo::OPERATION mode);
    void SetSpace(ImGuizmo::MODE mode);

    void ApplyToSelectedEntities();

private:
    ImGuizmo::OPERATION m_current_mode = ImGuizmo::TRANSLATE;
    ImGuizmo::MODE m_space = ImGuizmo::WORLD;
};
```

### 任务2.3：属性面板 - Transform
```
优先级：最高
工作量：2-3天

功能清单：
✓ 显示位置/旋转/缩放
✓ 实时编辑
✓ 数值精确输入
✓ 重置按钮
✓ 组件标题栏
✓ 折叠/展开
```

### 任务2.4：属性面板 - 材质编辑
```
优先级：高
工作量：3-4天

功能清单：
✓ 材质选择器
✓ 颜色编辑（Albedo、Emissive）
✓ 纹理槽（拖放纹理）
✓ 数值滑块（Metallic、Roughness）
✓ 实时预览
✓ 保存/应用按钮
```

### 任务2.5：属性面板 - Mesh 组件
```
优先级：中
工作量：2天

功能清单：
✓ Mesh 资源选择器
✓ 材质分配
✓ 阴影设置
✓ 渲染层级
```

### 任务2.6：场景层级窗口
```
优先级：中
工作量：2-3天

功能清单：
✓ 显示场景树
✓ 同步选择状态
✓ 展开/折叠子节点
✓ 拖拽重排序
✓ 右键菜单（复制、粘贴、删除）
✓ 搜索过滤
```

---

## 阶段三：场景保存/加载（第5周）

### 任务3.1：场景序列化
```
优先级：高
工作量：3-4天

功能清单：
✓ 场景数据结构定义
✓ 序列化为 JSON/XML
✓ 保存场景（所有实体、组件）
✓ 加载场景
✓ 场景版本控制
```

**数据格式示例**：
```json
{
  "scene": {
    "name": "Default Scene",
    "version": 1,
    "entities": [
      {
        "id": 0,
        "name": "Sphere",
        "transform": { "position": [0,1,0], "rotation": [0,0,0], "scale": [1,1,1] },
        "components": {
          "mesh": { "mesh_file": "sphere.mesh", "material_file": "sphere_0.material" }
        }
      }
    ]
  }
}
```

### 任务3.2：场景管理 UI
```
优先级：中
工作量：2天

功能清单：
✓ 新建场景
✓ 打开场景
✓ 保存场景
✓ 另存为
✓ 最近打开列表
✓ 保存提示（未保存时）
```

### 任务3.3：自动保存
```
优先级：低
工作量：1-2天

功能清单：
✓ 定时自动保存
✓ 可配置间隔
✓ 恢复上次编辑
✓ 自动保存提示
```

---

## 详细实施计划

### Week 1: 资源浏览器核心功能
- Day 1-2: 集成 ImGuizmo，搭建 ContentBrowser 类框架
- Day 3-4: 实现目录扫描和文件列表显示
- Day 5: 添加工具栏和路径导航

### Week 2: 资源预览和导入
- Day 1-2: 纹理预览功能
- Day 3-4: 模型预览功能（使用预览 RenderView）
- Day 5: 导入对话框基础

### Week 3: 场景选择和 Gizmo
- Day 1-2: 射线拾取实现
- Day 3-4: 选择系统集成
- Day 5: ImGuizmo 集成和基础测试

### Week 4: 属性面板和场景层级
- Day 1-2: Transform 属性编辑
- Day 3-4: 材质属性编辑
- Day 5: 场景层级窗口实现

### Week 5: 场景保存/加载
- Day 1-2: 场景序列化实现
- Day 3-4: 保存/加载 UI
- Day 5: 测试和修复

---

## 需要新增的文件结构

### 新增类文件
```
src/engine_runtime/dolas_function/
├── public/include/editor/
│   ├── ContentBrowser.h
│   ├── ResourceImporter.h
│   ├── ResourcePreviewManager.h
│   ├── EditorSelection.h
│   ├── GizmoController.h
│   ├── PropertyInspector.h
│   └── SceneSerializer.h
├── private/src/editor/
│   ├── ContentBrowser.cpp
│   ├── ResourceImporter.cpp
│   ├── ResourcePreviewManager.cpp
│   ├── EditorSelection.cpp
│   ├── GizmoController.cpp
│   ├── PropertyInspector.cpp
│   └── SceneSerializer.cpp
```

### 修改现有文件
```
src/engine_runtime/dolas_function/
├── public/include/manager/dolas_imgui_manager.h (添加 editor 模块引用)
├── private/src/manager/dolas_imgui_manager.cpp (连接新 editor 模块)
└── public/include/dolas_engine.h (添加 Editor 相关 Manager 指针)
```

### 新增第三方库
```
third_party/
└── imguizmo/  (或集成到现有 imgui 子模块中)
```

---

## 第一阶段完成后的功能

完成此阶段后，编辑器将具备以下功能：

1. ✅ **完整的资源管理**
   - 浏览所有项目资源
   - 导入新资源（FBX/OBJ/PNG等）
   - 预览纹理和模型
   - 拖放资源到场景

2. ✅ **基本的场景编辑**
   - 点击选择实体
   - 使用 Gizmo 移动/旋转/缩放（W/E/R 快捷键）
   - 编辑 Transform 和材质属性
   - 查看场景层级树

3. ✅ **场景持久化**
   - 保存和加载场景
   - 新建/打开场景
   - 最近打开列表

4. ✅ **专业编辑器体验**
   - UE 风格深色界面
   - 快捷键支持
   - 实时预览和反馈

---

## 后续开发方向

完成第一阶段后，可以考虑以下方向：

### 编辑器功能增强
- 组件系统完善（添加脚本、粒子系统等）
- 更多 Gizmo 工具（对齐、分布、地面吸附）
- 场景统计和分析工具
- 编辑器插件系统
- 性能分析和优化工具

### 渲染管线增强
- 阴影系统（阴影贴图、PCF）
- 后处理效果（Bloom、Tone Mapping）
- PBR 材质系统完善
- 延迟渲染优化
- 粒子系统和特效

### 游戏性功能
- 物理/碰撞系统（集成 PhysX 或自定义）
- 音频系统（FMOD 或 OpenAL）
- 脚本系统（Lua/Python）
- 动画系统
- AI 和寻路

---

## 技术参考

### 推荐资源
- [ImGui 官方文档](https://github.com/ocornut/imgui)
- [ImGuizmo](https://github.com/CedricGuillemet/ImGuizmo)
- [ImGui Docking Branch](https://github.com/ocornut/imgui/tree/docking)
- Unity Editor 文档（UI/UX 参考）
- Unreal Editor 文档（工作流参考）

### 设计原则
- 保持代码简洁，避免过度设计
- 优先实现核心功能，细节可以后续完善
- 注重用户体验（快捷键、实时反馈）
- 保持与现有代码风格一致
- 及时编写单元测试

---

## 版本历史

- **v1.0** (2026-03-10): 初始版本，定义第一阶段开发计划
